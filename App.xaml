<Application x:Class="AlphaPDF.App"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:local="clr-namespace:AlphaPDF">
    <Application.Resources>
        <ResourceDictionary>
            <ResourceDictionary.MergedDictionaries>
                <!-- [0] Theme (overridden at startup by ThemeManager.Initialize) -->
                <ResourceDictionary Source="Themes/Dark.xaml"/>
                <!-- [1] Strings (overridden at startup by LocaleManager.Initialize) -->
                <ResourceDictionary Source="Strings/en-US.xaml"/>
                <!-- [2] Shared fonts + type scale -->
                <ResourceDictionary Source="Themes/_Shared.xaml"/>
            </ResourceDictionary.MergedDictionaries>
        </ResourceDictionary>
    </Application.Resources>
</Application>
