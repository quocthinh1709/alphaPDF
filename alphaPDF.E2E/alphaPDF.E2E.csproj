<Project Sdk="Microsoft.NET.Sdk">

  <PropertyGroup>
    <OutputType>WinExe</OutputType>
    <TargetFramework>net48</TargetFramework>
    <PlatformTarget>x64</PlatformTarget>
    <LangVersion>latest</LangVersion>
    <Nullable>enable</Nullable>
    <ImplicitUsings>enable</ImplicitUsings>
    <IsPackable>false</IsPackable>
    <AssemblyName>alphaPDF.E2E</AssemblyName>
    <RootNamespace>alphaPDF.E2E</RootNamespace>
    <ApplicationIcon>alphaPDF.ico</ApplicationIcon>
  </PropertyGroup>

  <ItemGroup>
    <Content Include="alphaPDF.ico" />
  </ItemGroup>

  <ItemGroup>
    <PackageReference Include="FlaUI.Core" Version="4.0.0" />
    <PackageReference Include="FlaUI.UIA3" Version="4.0.0" />
    <PackageReference Include="PdfPig" Version="0.1.14" />
    <PackageReference Include="PdfSharpCore" Version="1.3.67" />
    <PackageReference Include="System.Text.Json" Version="8.0.5" />
  </ItemGroup>

  <ItemGroup>
    <!-- Screen.AllScreens, used to place the launched app window on a specific monitor
         (so it doesn't fight the developer's primary display for foreground). -->
    <Reference Include="System.Windows.Forms" />
  </ItemGroup>

</Project>
